name: test

on: workflow_dispatch

jobs:
  test:
    runs-on: ubuntu-latest
    container: pandoc/latex
    steps:
      - run: echo "files=$(printf '%s ' *.md)" > $GITHUB_OUTPUT
      - run: ${GITHUB_WORKSPACE}/.github/workflows/run.sh ${{ steps.files_list.outputs.files }}
